{
    "name": "@plugport/sqlite-compat",
    "version": "1.0.0",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsx watch src/index.ts",
        "test": "vitest run",
        "test:watch": "vitest",
        "lint": "eslint src/",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@plugport/shared": "workspace:*",
        "node-sql-parser": "^5.4.0"
    },
    "devDependencies": {
        "typescript": "^5.7.0",
        "tsx": "^4.19.0",
        "@types/node": "^22.0.0",
        "vitest": "^3.0.0"
    },
    "description": "Drop-in better-sqlite3 compatible SDK that uses PlugPort as the storage backend"
}
