# PlugPort Server Configuration
# Copy to .env in the server package root

HTTP_PORT=8080
WIRE_PORT=27017
HOST=0.0.0.0
LOG_LEVEL=info
METRICS_ENABLED=true
MAX_DOC_SIZE=1048576

# API key auth (leave blank to disable)
# API_KEY=your-secret-api-key

# MonadDb Production Storage
# Both must be set to enable production mode (writes cost MON gas)
# MONADDB_ENDPOINT=https://monaddb-rpc.monad.xyz/v1
# MONADDB_PRIVATE_KEY=your_64_char_hex_private_key
