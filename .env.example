# PlugPort Server Configuration

# ---- Server Ports ----
HTTP_PORT=8080
WIRE_PORT=27017
HOST=0.0.0.0

# ---- Authentication ----
# Set to enable API key auth on HTTP endpoints (leave blank to disable)
# API_KEY=your-secret-api-key

# ---- Logging ----
# Options: debug, info, warn, error
LOG_LEVEL=info

# ---- Metrics ----
# Set to false to disable Prometheus /metrics endpoint
METRICS_ENABLED=true

# ---- Document Limits ----
# Maximum document size in bytes (default: 1MB)
MAX_DOC_SIZE=1048576

# ---- Monad Testnet (Production Storage) ----
# Leave all blank to use in-memory storage (dev mode).
# Set all four to connect to Monad via smart contract (production mode).
# Writes cost MON gas. Reads are free.

# Monad testnet RPC endpoint
MONAD_RPC_URL=https://monad-testnet.drpc.org

# Monad testnet chain ID
MONAD_CHAIN_ID=10143

# Gas station wallet private key (64 hex chars, no 0x prefix)
# Must be the owner of the deployed PlugPortStore contract.
# Generate with: node -e "const w = require('ethers').Wallet.createRandom(); console.log('Private Key:', w.privateKey.slice(2)); console.log('Address:', w.address);"
# Fund the address with MON from https://faucet.monad.xyz before starting.
MONAD_PRIVATE_KEY=your_64_char_hex_private_key_here

# Deployed PlugPortStore contract address
MONAD_CONTRACT_ADDRESS=0xYourDeployedContractAddress
